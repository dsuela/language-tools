parameters:
    app.fixture_name: Symfony Language Tools

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Entity/'
            - '../src/Repository/'

    app.fixture_controller:
        class: App\Controller\HomeController

    App\Environment\CustomEnvVarProcessor:
        tags: ['container.env_var_processor']
